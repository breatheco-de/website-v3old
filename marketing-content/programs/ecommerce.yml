# Type-level ecommerce defaults for the "program" content type.
# These values are deep-merged with per-entry ecommerce.yml files.
# An entry must set purchasable: true to become a purchasable product.

purchasable: false
plans: []
