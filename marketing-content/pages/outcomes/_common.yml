# Common properties shared across all language variants
template: "default"
title: "online-platform"

meta:
  robots: "index, follow"
  priority: 0.8
  change_frequency: "weekly"

schema:
  include:
    - "organization"
    - "website"
