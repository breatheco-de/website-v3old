program:
  folder: programs
  url_pattern:
    en: /en/career-programs/:slug
    es: /es/programas-de-carrera/:slug

location:
  folder: locations
  url_pattern:
    en: /en/location/:slug
    es: /es/ubicacion/:slug

landing:
  folder: landings
  url_pattern:
    default: /landing/:slug

page:
  folder: pages
  url_pattern:
    en: /en/:slug
    es: /es/:slug
